import VirtualKeyboard from './VirtualKeyboard.svelte';

export default class VirtualKeyboardWrapper {
    layout!: [[string]];

    constructor() {}

    handleKeyPress(event) {
        // Handle key press event
    }
}